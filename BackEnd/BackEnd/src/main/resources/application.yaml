spring:
  application:
    name: PCF
  datasource:
    url: jdbc:postgresql://localhost:3000/PCF
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: 02022006

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8080
